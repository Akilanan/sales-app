# MOTION + PHYSICS + BUTTONS + EFFECTS AUDIT â€” prana-production-app (2026-06-11)

## (A) INVENTORY â€” what exists and is good

- **Token system** `src/lib/motion.js` â€” 5 spring presets, 4 easing curves, 5-step duration scale (0.12â€“0.45s), `tween()`/`exitTween()` builders. Damping ratios all in the target band: tap Î¶=0.75 (400/30), nav Î¶=0.87 (380/34), card Î¶=0.87 (300/30), modal Î¶=0.79 (300/30/m1.1), pop Î¶=0.72 (500/32). Genuinely instrument-grade tuning.
- **LazyMotion strict + async `domMax`** `src/main.jsx:18-21` + `src/lib/motion-features.js` â€” feature bundle loads post-first-paint; `MotionConfig reducedMotion="user"` wraps the whole app.
- **Reduced-motion coverage is strong**: global CSS kill-rule `src/index.css:36-45`, `motion-safe:` gates on smoke (`App.jsx:473-475`), scan-sweep (`ScrollExpandMedia.jsx:145`), ping dots, Suspense pulse; SMIL PulseDot gated by `REDUCED` (`App.jsx:267`); Spline robot gated `!REDUCED && isDesktop` (`ScrollExpandMedia.jsx:127`); `AnimatedNumber` manually gates the standalone `animate()` (which MotionConfig can't reach) `App.jsx:158-163`; the one-shot `spotlight` keyframe deliberately snaps to its lit end-state under reduce (fill-forwards, `index.css:93-98`).
- **Film grain** `src/index.css:63-72` â€” static inline-SVG feTurbulence, `opacity:0.028`, `mix-blend-mode:overlay`, fixed, pointer-none. Exactly per spec.
- **Tabular numerals** `src/index.css:122-127` â€” `.tnum/.font-mono/.font-display` get `tabular-nums slashed-zero` + `tnum/zero` features; mono font used on every KPI/data cell; count-up via MotionValueâ†’DOM (no re-render) on the glance banner + 4 KPI tiles (`App.jsx:157-166, 684, 2058`), animating from the previous value on live ticks.
- **Charts**: draw-in gated `isAnimationActive={!REDUCED}` with 700ms ease-out (`App.jsx:739, 775`); sparklines correctly `isAnimationActive={false}` (`App.jsx:830, 2075`); two bespoke dark tooltips (`ChartTip` App.jsx:106-135, `CompTip` :280-296) matching the spec-card system; live SMIL pulse dot on the trend's last point; deviation bars with per-corner-radius custom shape.
- **Progress meters animate `scaleX` (compositor), not width**: `Meter` `App.jsx:2039` and the glance banner bar `:697` set static `width%` and animate `scaleX 0â†’1` with `ease.out` â€” the classic width-animation jank trap correctly avoided.
- **Modals**: `spring.modal` + overlay fade, full ARIA dialog focus trap, Esc, focus restore (`App.jsx:1067-1087, 2010-2030`); backdrop click closes.
- **Nav**: tubelight `layoutId="lamp"` with `spring.nav`, `initial={false}` (no mount-flicker), mobile bottom thumb-bar variant (`tubelight-navbar.jsx:54-67`).
- **Page/tab transitions**: `AnimatePresence mode="wait"` keyed by view, fade + y10â†’0 / exit y-8 (`App.jsx:407-415`); loginâ†’app threshold uses a blur(8px) exit (`:382`).
- **Login**: Spline robot is lazy-chunked (`splite.jsx:7`), error-boundaried, gated to lg+ via matchMedia (prevents the display:none zero-size-canvas GL spam â€” documented at `ScrollExpandMedia.jsx:34-37`), drift-smoke is transform-only keyframes, scan-sweep is transform-only, spotlight beam is a one-shot.
- **Buttons**: MetalButton has real press physics (wrapper translateY(2.5px)+scale(0.99), inner scale(0.97), shadow drop, press shine, touch-device hover suppression, `transform-gpu`); LiquidButton has SVG-displacement glass + focus-visible ring; global `*:focus-visible` outline `index.css:105-108`.
- **PIN pad**: `whileTap scale 0.95` with `spring.tap`, pop-in PIN dots with `spring.pop` (`App.jsx:453-454, 507`).

## (B) FAIL / PARTIAL findings

1. **FAIL â€” `.spotlight` and `.border-beam` CSS classes do not exist; handlers run for nothing.** `App.jsx:145-149` (`spotlightMove` writes `--mx/--my`), consumed by class `spotlight` at `App.jsx:2055` (every KPI tile) and `:2085` (hero Panels), and `border-beam` at `:677` (glance banner when live). `index.css:5-8` confirms they were stripped. So: pointermove handlers fire on every cursor move over KPI/hero cards mutating CSS vars nothing reads, and the "traveling BorderBeam appears while live" comment (`:675-676`) describes a non-existent feature. **Fix:** add to index.css â€” `.spotlight::after{content:"";position:absolute;inset:0;border-radius:inherit;background:radial-gradient(180px circle at var(--mx) var(--my),rgba(255,255,255,0.06),transparent 65%);opacity:0;transition:opacity .2s;pointer-events:none}` `@media(hover:hover) and (pointer:fine){.spotlight:hover::after{opacity:1}}` plus a conic-gradient `.border-beam` keyframe (mask: two stacked backgrounds or an ::before rotating conic, transform-only); or delete the handlers/classes/comment.

2. **FAIL â€” no `@media(hover:hover)` gating anywhere; shop tablets get sticky hovers.** Zero hits in src. `Kpi` `whileHover={{y:-3}}` + `hover:border-brand-500/40 hover:shadow-card-hover` (`App.jsx:2054-2055`), LiquidButton `hover:scale-[1.03]` (`buttons.jsx:50`), and ~40 Tailwind `hover:` utilities all stick after a tap on touch. MetalButton alone self-gates via JS `"ontouchstart" in window` (`buttons.jsx:166-168`) â€” which also wrongly kills hover on touchscreen laptops with mice. **Fix:** `future: { hoverOnlyWhenSupported: true }` in `tailwind.config.js` (one line; needs dev-server restart per the project's known gotcha), replace `whileHover` on Kpi with a CSS-gated transform, and swap MetalButton's detection to `matchMedia("(hover:hover) and (pointer:fine)")`.

3. **FAIL â€” CursorSpotlight animates `left`/`top` per frame (layout thrash on the login).** `spotlight-cursor.jsx:22-23, 69` â€” springed MotionValues mapped to `left`/`top` styles; every mousemove over the sign-in panel triggers layout, on an element that also has `blur-xl`, inside a `backdrop-blur-xl` panel. **Fix:** `const x = useTransform(mouseX,(v)=>v-size/2)` and pass `style={{x, y, width:size, height:size}}` (transform-only; framer composes translate).

4. **FAIL â€” press feedback slower than 100ms on both flagship buttons.** MetalButton: `transition: "all 250ms cubic-bezier(0.1,0.4,0.2,1)"` (`buttons.jsx:116`) applies to press-down too, and `all` transitions box-shadow + filter (non-compositor). LiquidButton: `transition-transform duration-300` shared by hover and `active:scale-[0.99]` (`buttons.jsx:50`) â€” press lags 300ms. Tailwind-class buttons (`btnPrimary/btnSecondary` `App.jsx:1980-1983`, `Key` `:455`) use 200ms for active brightness. **Fix:** asymmetric timing â€” when pressed use ~60-80ms ease-out, release at 250ms (MetalButton: `transition: isPressed ? "transform 70ms ease-out, box-shadow 70ms ease-out" : ...`); LiquidButton add `active:duration-75`; also transition only `transform, box-shadow, filter` explicitly, never `all`.

5. **FAIL â€” MetalButton gives zero keyboard press feedback and can stick pressed.** Press state is mouse/touch-only (`buttons.jsx:180-186`); Enter/Space activation shows nothing, and there's no `onKeyDown/Up` or `:active` fallback. **Fix:** drive `isPressed` from CSS `:active` (group-active variants) or add key handlers; cheapest: move press transforms to `active:` Tailwind classes on the button + wrapper `group-active:`.

6. **FAIL â€” no skeletons; post-login "No data" flash.** `onLogin` (`App.jsx:333`) sets `user` then `await loadData()` â€” the dashboard mounts with empty arrays, so users see `Empty` panels ("Set monthly targetsâ€¦", `:719`) and 0-value KPIs for the fetch duration, then content pops with no transition. TeamAdmin shows plain "Loadingâ€¦" text (`:1218, 1233`). Boot screen is fine. **Fix:** add a `loading` flag to App state (set false after first `loadData`), render shimmer skeleton blocks (CSS `@keyframes shimmer` translate-only gradient, `motion-safe`) matching panel geometry, and stagger real panels in via the existing `containerV`.

7. **FAIL â€” Shift Entry save is fully network-blocking, not optimistic.** `doSave` (`App.jsx:955-969`): button shows "Savingâ€¦" â†’ `await db.addEntry` â†’ close â†’ toast â†’ `await reload()`. On shop-floor Wi-Fi this is the app's most-used interaction gated on two round-trips. Inline cell edits (`editQty :1538`, `editField :1807`, `setRate :1269`) silently swallow failures (`catch {/* keep */}`) â€” the input keeps a value that never saved. **Fix:** optimistically append the entry to local state + close + toast immediately, morph the confirm button label â†’ check (AnimatePresence swap), reconcile/rollback with an error toast on rejection; for cell edits add a transient saved-tick / red flash on failure.

8. **PARTIAL â€” tokens exist but ~12 call sites bypass them.** Hardcoded literals: `[0.22,1,0.36,1]` Ã—7 (`App.jsx:151, 382, 386, 408, 479` + icon nudges `:532, 1041`) vs token `ease.out=[0.16,1,0.3,1]`; durations 0.5/0.4/0.3/0.26/0.22 inline (`:151, 382, 386, 408, 503, 517`); inline spring `{stiffness:300,damping:24}` Î¶=0.69 in SplitReveal (`:189`); Recharts 700ms (`:739, 775`); MetalButton 250ms bezier (`buttons.jsx:116`); LiquidButton `duration-300`; toast and CredentialReveal use framer defaults (no transition at all, `:1091, 1128`). `dur.hover/dur.modal/dur.page`, `tween`, `exitTween` are never imported. **Fix:** sweep the literals to `tween(dur.page)`/`spring.*`, define one `EASE_OUT_CSS` string for Tailwind arbitrary values.

9. **PARTIAL â€” exits are not faster than entrances.** motion.js prescribes exits at 0.8Ã— with `ease.accel` (`motion.js:37, 48`), but the tab transition uses one 0.26s transition for enter and exit (`App.jsx:408`), login exit 0.3 vs app enter 0.4 (accidentally right), mode-switch 0.22 symmetric (`:503, 517`). `exitTween` is dead. **Fix:** `exit={{...}} transition={exitTween(dur.page)}` via per-target transitions (framer allows `transition` inside the `exit` object).

10. **PARTIAL â€” live-updating progress bars jump with no motion.** Static `width%` divs that re-render on every realtime reload: fleet mini-bars `App.jsx:893`, plan-vs-actual progress `:1360`, loading overview `:1557`, selected-machine bar `:1574`, Gantt bars `:1477`. Not jank (no animation at all), but live ticks teleport. **Fix:** convert to the `Meter` scaleX pattern, or add `transition-transform` with scaleX driven by pct (`transform: scaleX(pct/100)` on a full-width fill, `transform-origin:left`) â€” one class change per bar.

11. **PARTIAL â€” Spline robot has no LOW_POWER gate and never idles.** The `LOW_POWER` guard (`App.jsx:33-40`) only protects the unwired `HeroScene`; `ScrollExpandMedia.jsx:127-133` mounts the multi-MB cursor-tracking WebGL scene on ANY â‰¥1024px viewport â€” including cheap 1280Ã—800 shop tablets the guard was written for. The login also runs 4 infinite animators simultaneously (Spline rAF loop + smoke-a 22s + smoke-b 28s on two 140%-viewport `will-change-transform` layers `App.jsx:473-476` + scan-sweep 7s + animate-ping), stacked under `backdrop-blur-xl` Ã—2 â€” the login idles hotter than the whole dashboard. **Fix:** export/Ñ†ÐµÐ½Ñ‚Ñ€Ð°Ð»ize LOW_POWER and add it to the Spline mount condition; pause on `visibilitychange` (Spline runtime exposes `onLoad={(app)=>...}` â†’ call `app.stop()/play()`); consider `content-visibility` or removing one smoke layer.

12. **PARTIAL â€” toast/feedback motion is inconsistent and single-screen.** The only toast (`App.jsx:1090-1098`) animates with default framer tween (no token, no spring), 2200ms hardcoded dismiss, no progress indication; Settings save shows an inline 1500ms "Saved" label (`:1741`), costing/loading/routing cell edits give NO success feedback at all. **Fix:** one `<Toast/>` component with `spring.pop` enter / `exitTween(dur.pop)` exit, used by all mutation paths.

13. **FAIL â€” flagship chart labels request a font that is never loaded.** `DevLabel` uses `fontFamily="'IBM Plex Mono', ui-monospace, monospace"` (`App.jsx:253`) but `index.html` loads only IBM Plex Sans + JetBrains Mono â€” the deviation labels on chart 01 render in the OS fallback mono, breaking numeral consistency (and tabular-nums) on the most prominent chart. **Fix:** change to `'JetBrains Mono', ui-monospace, monospace`.

14. **Dead/stale motion code inventory.**
   - `SplitReveal` (`App.jsx:185-201`) â€” never rendered; carries its own inline spring + per-letter blur filter animation. Delete.
   - `useSpring` import (`App.jsx:2`) â€” unused.
   - `tween`/`exitTween` (`motion.js:47-48`) and `dur.hover/modal/page` â€” exported, never used (see #8/#9).
   - `pulse-dot` keyframes + animation (`tailwind.config.js:62-71`) â€” `animate-pulse-dot` appears nowhere in src.
   - `Sidebar` (`App.jsx:557-601`, intentionally kept) still contains `layoutId="navrail"` + `transition-[width]` (a width animation that would be jank if ever rewired).
   - `HeroScene`/`HeroErrorBoundary`/`LOW_POWER`/`Ambient` lazy import (`App.jsx:33-68` + `src/lib/Ambient.jsx`, 412 lines) â€” intentionally kept, but three.js/@react-three deps still ship as a build chunk.
   - Unused shadcn kit on disk: `src/components/ui/button.jsx`, `toggle.jsx`, `tooltip.jsx`, `card.jsx` (only `buttons.jsx` is imported anywhere).
   - Stale comments: `index.css:5-8` claims grain/scrollbar/spotlight "all stripped â€” plain scaffold" directly above the re-added grain and scrollbar rules; `App.jsx:675-676` describes the nonexistent BorderBeam; `App.jsx:143-144` claims the spotlight glow is "CSS-gated to fine-pointer desktops" (no such CSS); component name `ScrollExpandMedia` no longer scroll-expands anything.

15. **Checklist verdicts (condensed):** 1 spring physics PASS (tokens) / PARTIAL (bypasses, #8) Â· 2 duration tokens PARTIAL (#8/#9) Â· 3 non-compositor FAIL-light (only #3 CursorSpotlight + `transition:all` in MetalButton; meters/banners are clean) Â· 4 tabular+count-up PASS (gap: Today panel big numbers `:787-789` and shift cards `:797` are static, not `AnimatedNumber`) Â· 5 grain PASS Â· 6 hover gating FAIL (#2) Â· 7 chart motion PASS Â· 8 skeletons FAIL (#6) Â· 9 optimistic UI FAIL (#7) Â· 10 view transitions PASS-minimal (uniform fade-up, mode="wait" gap, no directionality) Â· 11 button physics PARTIAL (#4/#5; focus rings PASS) Â· 12 toasts PARTIAL (#12) Â· 13 reduced-motion PASS (caveat: module-scope `REDUCED` snapshots at load in 3 files â€” `App.jsx:28`, `ScrollExpandMedia.jsx:26-29`, `SplitReveal:186` â€” won't react to a runtime OS toggle) Â· 14 dead code FAIL (#14) Â· 15 login PARTIAL (#11; boot sequence stagger 0.45s Ã— 3 + 0.6 delay â‰ˆ 2.1s full reveal is deliberate and fine; Spotlight SVG keeps a permanent `will-change-transform` layer after its one-shot).

## (C) Top-10 upgrades, ranked by wow-per-effort

1. **Ship the missing `.spotlight` cursor-glow CSS** â€” the `--mx/--my` plumbing already exists on every KPI tile and hero panel; ~10 lines of CSS (gated `@media(hover:hover) and (pointer:fine)`) instantly add the Linear-style pointer sheen the code already promises. (Finding #1)
2. **Ship `.border-beam` on the glance banner** â€” a rotating conic-gradient hairline that runs only while `live` is true turns the realtime connection into a visible heartbeat; one keyframe + one pseudo-element, transform-only. (#1)
3. **Asymmetric button press timing** â€” 70ms press-down / 250ms spring release on MetalButton + LiquidButton + keyboard `:active`; this is the single biggest "feels expensive" lever for an app that is 90% button taps. (#4/#5)
4. **Optimistic Shift Entry + button morph** â€” instant local insert, confirm button morphs to a check (`AnimatePresence` icon swap with `spring.pop`), toast fires immediately; transforms the operators' core loop from "waiting on Wi-Fi" to instant. (#7)
5. **Skeleton shimmer for the loginâ†’dashboard handoff** â€” kill the "No data" flash with geometry-matched shimmer blocks + stagger-in of real panels via the existing `containerV`. (#6)
6. **`hoverOnlyWhenSupported: true`** â€” one Tailwind config line ends every sticky hover on the shop-floor tablets (plus restart). (#2)
7. **Animate the live progress bars to scaleX** â€” fleet, loading, plan-vs-actual bars glide on realtime ticks instead of teleporting; one pattern swap reused 5 places. (#10)
8. **Directional tab transitions + faster exits** â€” slide Â±16px on x based on tab-index delta (matching the tubelight lamp's travel direction) and wire the dead `exitTween` so exits run 0.8Ã—; makes nav feel spatial, ~20 lines. (#9)
9. **Login power management** â€” LOW_POWER gate on the Spline mount + `app.stop()` on `visibilitychange`/idle, drop one smoke layer; protects cheap tablets that sit on the login all shift. (#11)
10. **Token sweep + dead-code purge** â€” replace the 12 magic-number call sites with `spring/ease/dur/tween`, fix the `'IBM Plex Mono'` chart-label font, delete SplitReveal/pulse-dot/unused kit files/stale comments; locks the motion system so future edits can't drift. (#8/#13/#14)

Key files: `c:\Users\12aki\Downloads\prana-production-app\src\App.jsx`, `src\lib\motion.js`, `src\components\ui\buttons.jsx`, `src\components\ui\spotlight-cursor.jsx`, `src\components\ui\ScrollExpandMedia.jsx`, `src\index.css`, `tailwind.config.js`, `src\main.jsx`, `index.html`.
